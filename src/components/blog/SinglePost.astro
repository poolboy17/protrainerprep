---
import { Icon } from 'astro-icon/components';

import Image from '~/components/common/Image.astro';
import PostTags from '~/components/blog/Tags.astro';
import SocialShare from '~/components/common/SocialShare.astro';
import Breadcrumbs from '~/components/blog/Breadcrumbs.astro';
import ArticleSchema from '~/components/blog/ArticleSchema.astro';

import { getPermalink } from '~/utils/permalinks';
import { getFormattedDate } from '~/utils/utils';
import { getBreadcrumbs } from '~/utils/breadcrumbs';

import type { Post } from '~/types';

export interface Props {
  post: Post;
  url: string | URL;
}

const { post, url } = Astro.props;

const breadcrumbItems = getBreadcrumbs(post.slug, post.title);
---

<ArticleSchema
  title={post.title}
  description={post.excerpt}
  publishDate={post.publishDate}
  updateDate={post.updateDate}
  author={post.author}
  image={typeof post.image === 'string' ? post.image : undefined}
  url={String(url)}
/>

<!-- Hero header with dark slate background -->
<div class="bg-gray-900 dark:bg-gray-950 pt-8 sm:pt-12 pb-8">
  <div class="max-w-3xl mx-auto px-4 sm:px-6">
    <Breadcrumbs items={breadcrumbItems} />

    <h1 class="text-4xl md:text-5xl font-bold leading-tighter tracking-tighter font-heading mt-2 text-white">
      {post.title}
    </h1>

    <p class="mt-4 mb-5 text-lg md:text-xl text-gray-300 leading-relaxed">
      {post.excerpt}
    </p>

    <!-- Meta bar -->
    <div class="flex flex-wrap items-center gap-x-3 gap-y-1.5 text-sm text-gray-400">
      <span class="flex items-center gap-1.5">
        <Icon name="tabler:calendar" class="w-4 h-4 text-gray-500" />
        <time datetime={String(post.publishDate)}>{getFormattedDate(post.publishDate)}</time>
      </span>
      {
        post.author && (
          <span class="flex items-center gap-1.5">
            <span class="text-gray-600">·</span>
            <Icon name="tabler:user" class="w-4 h-4 text-gray-500" />
            <span>{post.author}</span>
          </span>
        )
      }
      {
        post.category && (
          <span class="flex items-center gap-1.5">
            <span class="text-gray-600">·</span>
            <a
              href={getPermalink(post.category.slug, 'category')}
              class="flex items-center gap-1 text-green-400 hover:text-green-300 hover:underline font-medium"
            >
              <Icon name="tabler:tag" class="w-3.5 h-3.5" />
              {post.category.title}
            </a>
          </span>
        )
      }
      {
        post.readingTime && (
          <span class="flex items-center gap-1.5">
            <span class="text-gray-600">·</span>
            <Icon name="tabler:clock" class="w-4 h-4 text-gray-500" />
            <span>{post.readingTime} min read</span>
          </span>
        )
      }
    </div>
  </div>
</div>

<section class="mx-auto">
  <article>
    <!-- Hero image: overlaps the dark header slightly -->
    {
      post.image ? (
        <div class="-mt-2">
          <Image
            src={post.image}
            class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700 shadow-lg"
            widths={[400, 900]}
            sizes="(max-width: 900px) 400px, 900px"
            alt={post?.excerpt || ''}
            width={900}
            height={506}
            loading="eager"
            decoding="async"
          />
        </div>
      ) : (
        <div class="max-w-3xl mx-auto px-4 sm:px-6 mt-6">
          <div class="border-t dark:border-slate-700" />
        </div>
      )
    }

    <!-- Article body -->
    <div
      class="article-body mx-auto px-6 sm:px-6 max-w-3xl prose prose-md lg:prose-xl dark:prose-invert dark:prose-headings:text-slate-300 prose-headings:font-heading prose-headings:leading-tighter prose-headings:tracking-tighter prose-headings:font-bold prose-a:text-primary dark:prose-a:text-blue-400 prose-img:rounded-md prose-img:shadow-lg mt-8 prose-headings:scroll-mt-[80px] prose-li:my-0"
    >
      <slot />
    </div>
    <div class="mx-auto px-6 sm:px-6 max-w-3xl mt-8 flex justify-between flex-col sm:flex-row">
      <PostTags tags={post.tags} class="mr-5 rtl:mr-0 rtl:ml-5" />
      <SocialShare url={url} text={post.title} class="mt-5 sm:mt-1 align-middle text-gray-500 dark:text-slate-600" />
    </div>
  </article>
</section>

<style is:global>
  /* Alternating H2 section backgrounds — visible contrast */
  .article-body > h2:nth-of-type(even) {
    background: #f0fdf4;
    margin-left: -1.5rem;
    margin-right: -1.5rem;
    padding: 0.875rem 1.5rem 0.625rem;
    border-radius: 0.5rem;
    border-left: 4px solid #16a34a;
  }

  .article-body > h2:nth-of-type(odd) {
    background: #f8fafc;
    margin-left: -1.5rem;
    margin-right: -1.5rem;
    padding: 0.875rem 1.5rem 0.625rem;
    border-radius: 0.5rem;
    border-left: 4px solid #334155;
  }

  @media (prefers-color-scheme: dark) {
    .article-body > h2:nth-of-type(even) {
      background: rgba(22, 163, 74, 0.1);
      border-left-color: #22c55e;
    }
    .article-body > h2:nth-of-type(odd) {
      background: rgba(51, 65, 85, 0.3);
      border-left-color: #64748b;
    }
  }

  /* Inline images: generous spacing */
  .article-body > p > img {
    margin-top: 2rem;
    margin-bottom: 2rem;
  }

  /* Full-width color band behind every 3rd H2 section */
  .article-body > h2:nth-of-type(3n) {
    background: #1e293b;
    color: #f1f5f9 !important;
    margin-left: -1.5rem;
    margin-right: -1.5rem;
    padding: 1rem 1.5rem 0.75rem;
    border-radius: 0.5rem;
    border-left: 4px solid #22c55e;
  }

  @media (prefers-color-scheme: dark) {
    .article-body > h2:nth-of-type(3n) {
      background: rgba(15, 23, 42, 0.8);
      color: #e2e8f0 !important;
    }
  }
</style>
