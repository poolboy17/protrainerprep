---
export interface Props {
  title?: string;
  items: string[];
}

const { title = 'Your Checklist', items = [] } = Astro.props;
---

<div class="not-prose my-8 rounded-xl border border-indigo-200 bg-white p-5 shadow-sm">
  <h3 class="flex items-center gap-2 text-base font-bold text-gray-900 mb-4">
    <svg class="h-5 w-5 text-[#4f46e5]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
    </svg>
    {title}
  </h3>
  <ul class="space-y-3">
    {items.map((item) => (
      <li class="flex items-start gap-3 text-sm text-gray-700">
        <div class="flex-shrink-0 mt-0.5 w-5 h-5 rounded border-2 border-gray-300 flex items-center justify-center">
          <span class="text-transparent">✓</span>
        </div>
        <span>{item}</span>
      </li>
    ))}
  </ul>
</div>
